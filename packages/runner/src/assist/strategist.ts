import type { StrategistContext, StrategistFn, PlannedChunk } from "./types.js";

/**
 * Strategist es una función inyectada por el API (DI). Aquí exponemos sólo los tipos
 * y un helper para construir el contexto desde el runner.
 */
export type { StrategistContext, StrategistFn, PlannedChunk };
